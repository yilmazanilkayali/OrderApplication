@using OrderApplication.Entities
@model IEnumerable<OrderProduct>

    <div class="container">
    <div class="row">
        <div class="col-6">
            <h2>Order List</h2>
        </div>
        <div class="col-6">
          
        </div>
    </div>
    <div class="row">
        <table class="table table-bordered table-striped my-2 table-hover">
            <thead>
                <tr>
                    <th>
                        Order ID
                    </th>
                     <th>
                        FullName
                    </th>
                     <th>
                        Price
                    </th>
                    <th >
                        Status
                    </th>
                    <th >
                        
                    </th>
                </tr>
            </thead>
            <tbody>                
                @foreach (var orderProduct in Model)
                {
                    <tr>
                        <td class="pt-4">@orderProduct.Id</td>
                        <td class="pt-4">@orderProduct.Name</td>
                        <td classs="pt-4">@orderProduct.OrderPrice</td> 
                        <td class="pt-4">@orderProduct.OrderStatus</td>
                        <td>
                            <a asp-area="Admin" asp-controller="Order" asp-action="Details" asp-route-id="@orderProduct.Id" class="btn btn-primary">
                                Details
                            </a>
                            
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>